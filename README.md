# Dinazor
Dinazor oyunu
<!DOCTYPE html><html lang="tr"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Dino Run Pro</title><style> body{ margin:0; overflow:hidden; font-family:Arial, sans-serif; background:#87CEEB; transition: background 0.5s; user-select: none; } #game{ width:100vw; height:100vh; position:relative; } #ground{ position:absolute; bottom:0; width:100%; height:100px; background:#c2b280; border-top: 4px solid #8B7355; } #dino{ position:absolute; bottom:100px; left:80px; width:60px; height:60px; background:green; border-radius:10px; transition: transform 0.1s; } #cactus{ position:absolute; bottom:100px; left: 100%; width:35px; height:70px; background:brown; border-radius: 4px; } #score,#lives,#record{ position:absolute; left:20px; font-size:22px; font-weight:bold; color: #333; text-shadow: 1px 1px 2px rgba(255,255,255,0.8); z-index: 10; } #score{top:10px;} #lives{top:40px;} #record{top:70px;} .dark-mode { background: #222 !important; } .dark-mode #score, .dark-mode #lives, .dark-mode #record { color: #fff; text-shadow: 1px 1px 2px rgba(0,0,0,0.8); } </style></head><body><div id="game"> <div id="score">Skor: 0</div> <div id="lives">Can: 3</div> <div id="record">Rekor: 0</div><div id="dino"></div> <div id="cactus"></div> <div id="ground"></div> </div><script> const dino = document.getElementById("dino"); const cactus = document.getElementById("cactus"); const scoreEl = document.getElementById("score"); const livesEl = document.getElementById("lives"); const recordEl = document.getElementById("record"); let score = 0; let lives = 3; let speed = 8; let isJumping = false; let dinoVelocityY = 0; let dinoBottom = 100; const gravity = 1.2; const jumpForce = 18; const baseGround = 100; let cactusX = window.innerWidth; let record = localStorage.getItem("record") || 0; recordEl.innerText = "Rekor: " + record; // --- Gelişmiş Fizik Tabanlı Zıplama --- function jump() { if (!isJumping) { isJumping = true; dinoVelocityY = jumpForce; } } function updatePhysics() { if (isJumping) { dinoVelocityY -= gravity; dinoBottom += dinoVelocityY; if (dinoBottom <= baseGround) { dinoBottom = baseGround; dinoVelocityY = 0; isJumping = false; } dino.style.bottom = dinoBottom + "px"; } requestAnimationFrame(updatePhysics); } requestAnimationFrame(updatePhysics); // --- Kontroller --- document.addEventListener("keydown", (e) => { if (e.code === "Space" || e.code === "ArrowUp") { e.preventDefault(); jump(); } }); document.addEventListener("touchstart", (e) => { e.preventDefault(); jump(); }, { passive: false }); // --- Ana Oyun Döngüsü --- function gameLoop() { // Kaktüs Hareketi cactusX -= speed; if (cactusX < -50) { cactusX = window.innerWidth; score += 10; scoreEl.innerText = "Skor: " + score; // Zorluk Seviyesi Artışı (Her 50 skorda bir hafif hızlanma) if (score % 50 === 0) { speed += 0.8; } } cactus.style.left = cactusX + "px"; // --- Hassas Dinamik Çarpışma Testi (AABB Collision) --- const dinoRect = dino.getBoundingClientRect(); const cactusRect = cactus.getBoundingClientRect(); if ( dinoRect.right > cactusRect.left && dinoRect.left < cactusRect.right && dinoRect.bottom > cactusRect.top && dinoRect.top < cactusRect.bottom ) { lives--; livesEl.innerText = "Can: " + lives; if(navigator.vibrate) navigator.vibrate(300); cactusX = window.innerWidth; // Çarptıktan sonra kaktüsü sıfırla (arka arkaya can gitmesin) if (lives <= 0) { if (score > record) { localStorage.setItem("record", score); } alert("Oyun Bitti!\nSkor: " + score); score = 0; lives = 3; speed = 8; cactusX = window.innerWidth; scoreEl.innerText = "Skor: " + score; livesEl.innerText = "Can: " + lives; record = localStorage.getItem("record") || 0; recordEl.innerText = "Rekor: " + record; } } requestAnimationFrame(gameLoop); } requestAnimationFrame(gameLoop); // --- Gece/Gündüz Döngüsü --- setInterval(() => { document.body.classList.toggle("dark-mode"); }, 30000); // Pencere boyutu değiştiğinde kaktüsün dışarıda kalmasını engelle window.addEventListener('resize', () => { if(cactusX > window.innerWidth) cactusX = window.innerWidth; }); </script></body> </html>
